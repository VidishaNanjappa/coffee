# Coorg Cup backend

This directory contains the future API service for products, authentication, carts, orders, payments, and admin operations.

Keep backend dependencies, environment variables, routes, and persistence inside this directory.